const Toast = () => {
    return (
        <div className="toast toast-top toast-end">
  <div className="alert alert-info">
    <span>Cannot make the same entry more than once</span>
  </div>

</div>
    );
};

export default Toast;